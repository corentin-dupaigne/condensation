apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth-svc
  name: auth-svc
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: auth-svc
  type: ClusterIP
status:
  loadBalancer: {}
