name: CD Prod — Auth

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      image-tag:
        description: "Tag to apply to the image (defaults to the git tag)"
        required: false
        default: "test"

jobs:
  build-push-scan:
    name: Build, push & scan
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/_docker-build.yaml
    with:
      image-name: ghcr.io/${{ github.repository_owner }}/fullstack_project/auth
      image-tag: ${{ inputs.image-tag || github.ref_name }}
      also-tag-latest: true
      dockerfile-context: ./authentication
    secrets:
      GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
