name: CI — Frontend

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Test & audit
    uses: ./.github/workflows/_test-node.yaml
    with:
      working-directory: ./frontend
